query GetSpendsByRange(
  $orderByRange: OrderByRange!
  $limit: Int!
  $cursor: String
) {
  getSpendsByRange(
    orderByRange: $orderByRange
    limit: $limit
    cursor: $cursor
  ) {
    id
    name
    description
    amount
    date
    category {
      id
      name
    }
  }
}
